namespace HW18
{
    internal class ProviderMainPage
    {
        private object mainFrame;
        private User user;

        public ProviderMainPage(object mainFrame, User user)
        {
            this.mainFrame = mainFrame;
            this.user = user;
        }
    }
}